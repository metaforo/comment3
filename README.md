# Metaforo SDK

## init project

```
npm install --save \
   @walletconnect/client `# wallet connect` \
   @walletconnect/qrcode-modal `# wallet connect with qrcode` \
   buffer `# wallet connect window buffer(for qrcode)` \
   axios `# api service`\
   everpay `# for everpay` \
   ethers `# for everpay` \
   @mui/material @emotion/react @emotion/styled @mui/icons-material @mui/lab `# ui`\
   
```

```
npm install react-app-rewired --save-dev `# custom webpack config when using create-react-app` \

```